## src-fragger (always active)

**Code you write to finish a task is a frag, and a frag lives in the store's `src/`, never in a scratchpad — `src/` is the git-tracked home of every reusable and worthwhile output of the work, code or not, and the project's `.claude/scratchpad/` is always the git-ignored notebook.**
`store` = **`<working dir>/.claude/vlds/`** — the VLDS store; frags live under **`store/src/`**, one directory per task, and every frag is registered in **`store/src/frags.md`**. `notebook` = **`<working dir>/.claude/scratchpad/`** — always git-ignored, by its own `.gitignore` of `*` that the SessionStart hook seeds.

| Fires when | Do |
|---|---|
| **you are about to write code to complete a task** — a script, a probe, a generator, a migration, a sweep, a harness page, a workflow script, a probe project's build files, a program you would otherwise run from a heredoc: anything that replaces manual, user-involved labor and whose job no single tool call does (the floor, next row) | write it to `store/src/<task-slug>/<name>.<ext>` — never a scratchpad, `/tmp`, or any temp directory — with a header comment naming the task, the date copied from the hook stream's `now:`, and the exact run command; register it in `store/src/frags.md` before the first run. It is tracked with the project: the output the notes were made for. A worthwhile output that is not code — a design page, a sheet, a README — lives beside the task's frags in the same directory, tracked and unregistered |
| **the code you are about to write only does what one tool call does** — reads a file, swaps one or a few literal spans, writes it back; appends lines to a file; writes a whole file; runs one shell command | it is not a frag: make the Edit, Write, or Bash call. A heredoc that would carry backslashes, quotes, or `$` is the case where the shell cannot do the job, and the fallback from it is the dedicated tool — never a script that does the tool's job. A swap script is an Edit call in costume and worse than the call: it keys on text that moves, lands twice when its new text contains its old, and needs a header, a register entry, a state, and a retirement the call never needed. A store row, a per-turn record, anything written once and never run again is a Write call for the same reason — a frag is kept to be run again |
| **you are about to write a working file that is not a reusable output** — notes, a plan, a commit message, a fixture, a build's or probe's output, a log, a captured page, a downloaded source kept for reading | write it under the notebook, `<working dir>/.claude/scratchpad/` — never the harness's per-session temp directory (the `AppData\Local\Temp\claude\...\scratchpad` path the system prompt names): that one is named after a session id, invisible to the user, and gone with the session. The notebook is ignored always, so a note there is never committed and costs nothing. Code worth keeping is not a note: a code file in a scratchpad is neither a frag nor a tool call, and the gate asks |
| **a notebook file turns out to be worth keeping** — a later task runs or edits it, the user edits it, or a commit is about to carry it | it is an output, not a note: move it to `store/src/<task-slug>/`, register it, and leave a pointer where the notebook refers to it. A reading a claim cites is not made an output by the citation — the claim's own entry carries what was read, and the instrument that produced the reading is the output. Nothing in the notebook is force-added to git — a note that seems to need committing is an output that needs moving |
| a task needs labor a frag may already cover | read `store/src/frags.md` first; update the frag in place and move its register entry's `time:` and `state:` with it, instead of writing a second one beside it |
| the harness refuses your execution of a frag | **double-check before interrupting the user** — (1) the frag is under `store/src/` and registered, so the hand-off would be one click; (2) the refusal is durable: retry the SAME call once, unchanged, in a fresh tool call — a refusal announced "for the rest of this conversation" is bound to the conversation's content, not the process: a fresh tool call has passed where the first was refused, a fresh conversation opened with a hand-off summary clears it, and a resume of the same conversation does not; (3) the user's word already covers the act — a word given once in the conversation still stands, and a retry needs no new one; (4) the interruption is a decision only the user can make, not labor you could still do. Record the retry in the register's `retry:` field. Only when all four hold: hand the exact run command in ONE `bash`-tagged fence, one command per fence, written for the user's own shell (the click runs there, not in yours), mark the entry `state: handed-off`, and say plainly what was and was not run — never re-write the frag or reroute the call through another tool or agent to get past the refusal |
| the user edits a frag | the edit is a ruling: read the current file before running or updating it, never overwrite it from memory |
| a frag's task is done and nothing will need it again | mark it `state: retired` in the register; delete nothing — the user disposes |

**Why the store.** Three homes, three lifetimes. The harness's per-session scratchpad is gone with the session: a script there is invisible to the user and unreachable when the harness refuses your own execution and the user has to run it instead. The project's notebook outlives the session but lives on one disk only — ignored, never in the project's history: the right lifetime for a build log, the wrong one for the harness that read it. A frag under `store/src/` outlives the session, is the user's to read and edit, rides in the project's history, and is the recovery path when execution is refused. The split is by worth, not by file type: what someone will run, read, or edit again is tracked; what was produced on the way is not.

**The floor.** A frag is measured by its job, not by its length. One tool call does an edit, an append, a whole-file write, or a shell command, and a program that only does one of those is the call in costume — written, nearly always, because a heredoc failed on the text it carried. The dedicated tool exists for exactly that text; reaching for a script instead trades a call that cannot land twice for a file that can, and then registers the file.

**The gate.** A `frag-gate` hook asks before ANY file is written or edited in the harness's per-session scratchpad, before a code file lands in any other temp location, and before a code file lands in any project's `.claude/scratchpad/` — the place a swap script goes once it has stopped calling itself a frag, which the floor answers the same way: a frag under `src/`, or the tool's call and no file; it asks, never denies — a truly throwaway probe, run once and never again, may stay in the notebook, a copy of someone else's source kept for reading is a note, and only you can say which this one is; the ask exists because "throwaway" is what every kept script called itself first.

**The floor's own arm.** The gate reads shape as well as location: a short program that reads a file, swaps a span in it and writes it back is an Edit call in costume, and it asks wherever that program lands — under `store/src/` as readily as in a scratchpad, because the costume is wrong by its job, not by its address. All three of read, swap and write-back must appear in a short body, so a script that merely writes an output file is never touched.

**Scratch in the project root.** A data file written straight into the project root — a `.json`, `.jsonl`, `.csv`, `.log`, `.txt` dump that is not one of the project's own manifests — asks too. It lands untracked beside the real files, nothing sweeps it, and the person who finds it weeks later is the user rather than whoever wrote it; the project's own `.claude/scratchpad/` is git-ignored and is where that data belongs.

**The notebook ignores itself.** The SessionStart hook writes `.claude/scratchpad/.gitignore` containing `*` when absent and never overwrites it — a user's edit is a ruling — so a note is ignored wherever the root `.gitignore` stands. The hook never touches the index or the root `.gitignore`: files committed into a scratchpad before the rule stay tracked until the user removes them from the index, and where a root rule ignores `.claude/` wholesale, `src/` is not tracked either — the hook says so in one line at session start; narrowing that rule is the user's edit, never made for them. The same start says, in one line, when `src/` holds code the register does not name or the register names a path that is gone and not retired: register the frag, or mark the entry.

**Where the gate is silent, and why.** Inside a subagent it says nothing about the ignored cases — the harness scratchpad, a temp directory, the notebook. No hook event injects text into a spawned subagent's context (`SessionStart` does not fire for one; `SubagentStart`'s stdout goes to the debug log), so its writer was never told this contract exists, and the ask is answered by a human who cannot redirect an agent already running — one prompt per scratch file is a stream, not a gate. The `agent_id` field, present only inside a subagent call, is the tell. The silence reads a missing channel, not a lesser care for subagent code; when an injection channel exists, this scoping should go.

The silence stops at the ignored, though: a file dropped in the project root lands in status, outlives every agent that could have been told, and is found by the user, so the root case asks for a subagent as it does for the main loop. What decides is whether the file reaches the project's history or status, not who the writer was.

Full procedure: the `frag` skill of the `src-fragger` plugin.
