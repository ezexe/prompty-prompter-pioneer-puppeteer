// Template — copy to `.claude-plugin/marketplace.json`, then strip these comments.
// A marketplace lists one or more plugins. `source: "./"` means the plugin lives at this repo's
// root (the common single-plugin case). For plugins in subdirectories use "./path/to/plugin",
// and for external repos use a {"source": "github", "repo": "owner/name"} object instead.
{
  "name": "[marketplace-name]",
  "owner": { "name": "[you]", "url": "https://github.com/[you]" },
  "plugins": [
    {
      "name": "[plugin-name]",
      "source": "./",
      "description": "[one-line description]",
      "version": "0.1.0"
    }
  ]
}
