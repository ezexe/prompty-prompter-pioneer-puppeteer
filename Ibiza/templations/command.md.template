---
description: [one line shown in the slash-command menu]
# argument-hint: "<file>"            # optional usage hint shown after the command name
# allowed-tools: [Read, Edit, Bash]  # optional tool allowlist for this command
---

<!-- Template — copy to `commands/[command-name].md` at the plugin root, then delete this note.
     Invoked as /[plugin-name]:[command-name]. A command is a manual-only prompt (no auto-trigger).
     Reference invocation arguments with $ARGUMENTS, or $1 / $2 for positional args. -->

# /[command-name]

[The prompt body that runs when the user invokes the command. Write it as instructions to Claude,
exactly as you would phrase the task yourself.]
